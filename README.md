# RebirthProjectMU

MU Online client + server source (Season 5.2~6.3), `Source_Client` (C++/OpenGL) and `Source_Server` (DataServer, JoinServer, ConnectServer, GameServer).

## Changelog

### 2026-08-17
- **Fix (data)**: after the Lorencia fix below, scanned all 91 loaded map `.att` files (`MuServer/Data/Terrain/Terrain*.att`) for the same pattern - an enclosed hole (flood-filled connected component of non-safezone cells with every outward neighbor already safezone-flagged) - and cross-checked each candidate's walkability (`CheckAttr(x,y,4)`/`(x,y,8)`, wall/no-ground bits) to separate real bugs from legitimate building/wall footprints that correctly aren't safezone. Found one more genuine, fully-walkable hole: `Terrain7.att` (`MAP_ARENA`, index 6) at X:237-243,Y:115-119, 35/35 walkable cells. Patched the same way - only the missing-bit cells inside the exact enclosed component, nothing else touched. (Two other candidates initially reported in Lorencia turned out to be a false positive from an early version of the walkability check that scanned the hole's bounding box instead of its actual flood-filled cells - re-verified against the real component and both are 100% wall footprint, correctly not safezone, left untouched.)
- **Fix (data)**: `MuServer/Data/Terrain/Terrain1.att` (not tracked in git - live server data) - `/abrirbau` (`CommandOpenWarehouse`, gated by `CommandOpenWareOnlySafeZone=1` in `GameServerInfo - Command.dat`) was refusing to open near the Lorencia fountain (Evan-reported coordinates X:140-143,Y:130). Parsed the live `.att` file directly (3-byte header + 256x256 attribute grid, `CheckAttr(x,y,1)` = safezone bit) and flood-filled from a seed inside the reported area: found a perfectly enclosed 11x11 rectangular hole at X:140-150,Y:123-133 - walkable ground with the safezone bit missing, sitting dead-center in an otherwise fully-safezone plaza (everything on all 4 sides already flagged). Patched all 121 cells in that exact rectangle, verified the 4 reported coordinates now read `safezone=1` and that the surrounding already-safezone boundary was untouched. **This file is only read once at boot (`ReadMapInfo`) - needs a GameServer restart to take effect.** **Confirmed working in-game by Evan.**
- **Feature**: `LuaFunction.cpp/.h` (GameServer) - closed out the last gap in the Lua GameServer survey (`LuaTrade.cpp`, `LuaWarehouse.cpp`, `LuaChaosBox.cpp` from "RoxGaming Main 5.2 - 60 FPS UPDATE"): all three are the exact same OOP item wrapper as `LuaInventory.cpp`, just pointed at `gObj[].Trade[]`/`.Warehouse[]`/`.ChaosBox[]` instead of `.Inventory[]` - confirmed all three arrays exist in this project's `User.h` (`CItem* Trade`, `CItem* Warehouse`, `CItem* ChaosBox`). Ported the same 7-function set already done for Inventory (`GetItemTable`, `SetItemOption`, `SetItemDurability`, `ConvertItem`, `IsPeriodicItem`, `GetPeriodicItemTime`, `IsExcItem`) to all three - 21 new free functions total, same bounds-checked (`OBJECT_RANGE`/`OBJECT_USER`) pattern, registered in `InitLuaFunction()` as `Trade*`/`Warehouse*`/`ChaosBox*`. Also formally verified `LuaDatabase.cpp` (RoxGaming's `gQueryManager`-based SQL wrapper) as fully redundant with this project's existing 10 `LuaSQL*` functions - no port needed. This closes the GameServer-side Lua survey: all 14 `Lua*.cpp` files under that pack's `Source/GameServer/GameServer` now have a documented disposition (ported, redundant, or explicitly skipped with a reason) across this changelog's 2026-08-17 entries. Self-build-verified (`Release_EX603`/`Release_EX603CS`, 0 errors) and redeployed to `MuServer`. **Confirmed working in-game by Evan.**
- **Fix (crash)**: `CCustomNpcCollector::Load` (`CustomNpcCollector.cpp`, GameServer) - real, pre-existing bug, unrelated to any of this session's Lua work, found by resolving a live crash dump (`dbghelp.dll` symbol lookup against the freshly-built PDB, no `cdb`/WinDbg available in this environment) straight to this function. Three real defects in the same loop: (1) the array-init loop wrote `m_Data[m_count]` instead of `m_Data[n]` for all `MAX_ITEM_LIST` iterations - only ever touched one slot, the rest of the array was never actually initialized; (2) the parse loop wrote `m_Data[m_count]` and incremented `m_count` with **no bound check** against `MAX_ITEM_LIST` (50) - a data file with more entries than the array holds silently overran it (heap/stack corruption - this crashed once this session's much-larger rebuild shifted memory layout enough for the overrun to actually hit something, the classic "recompile reveals a latent bug" pattern already seen a few times in this project's history); (3) the inner parse loop had no `else` branch for a nonzero section id - would've spun forever instead of skipping to the next section. All three fixed. Also added the missing `GAMESERVER_TYPE`/`GAMESERVER_UPDATE` preprocessor definitions to the `(GameServer)Debug` configuration in `GameServer.vcxproj` (only the two Release configs had them - Debug never had them at all, breaking any code gated on those macros, e.g. `CMasterSkillTree::CheckMasterLevel` compiling as an empty non-void function). Rebuilt `Release_EX603`/`Release_EX603CS` (0 errors) and redeployed both to `MuServer`. **Confirmed stable in-game by Evan.**
  - Getting the server to actually boot past this also required a cascade of **data-file** fixes (not tracked in git - `MuServer/Data/` is gitignored, live server data) in `MuServer\Data\Custom\`: `CustomNpcCollector.txt`, `CustomRankUser.txt`, `CustomQuiz.txt`, `CustomRanking.txt`, `CustomNpcQuest.txt`, `CustomQuest.txt`, and `CustomStartItem.txt` were all missing the leading bare-number "section id" line their own `CMemScript`-based `.cpp` loader expects before each data block (confirmed present in a known-good sibling file, `CustomQuiz.txt`'s own first two sections) - without it, `CMemScript::GetToken()`'s own 1-second no-progress watchdog eventually throws, producing the `"[path] The file were not configured correctly"` dialog. Added the missing marker to each. Separately, `CustomQuiz.txt`/`CustomRankUser.txt` had a handful of Portuguese-accented characters permanently corrupted to a literal Unicode replacement character (U+FFFD, baked into the file as genuine UTF-8 bytes at some undetermined earlier point, unrecoverable) - reconstructed the 5 that were actually visible to players (4 quiz questions, 1 rank title) from context, then **converted all 7 touched files from UTF-8 to ANSI/CP1252** (per a community-confirmed fix Evan found - this client's `Main.dll` renders text as single-byte ANSI, not UTF-8, so even correctly-spelled accented text stays garbled in-game unless the file itself is saved as ANSI). A follow-up scan of all 234 `.txt` files under `Data\` found no other encoding problems - the 6 other files with accented text (`Message.txt` included, 661 accented bytes) were already correctly ANSI-encoded; this UTF-8 drift was isolated to the 7 already-touched files.
- **Feature**: `LuaFunction.cpp/.h` (GameServer) - 4 more bindings finishing the `LuaFunctions.cpp` survey: `DamageSend`, `RefreshCharacter` (`GCNewCharacterInfoSend`), `LevelExperience` (`gLevelExperience[]` lookup), `TeleportMapRange` (random position within a map, then a real teleport - `Map::GetMapRandomPos` + `gObjTeleport`). Verified every underlying function/global against this project's real headers first - and it turned out the overwhelming majority of what was left in `LuaFunctions.cpp` was either already covered by this project's own existing bindings under different names (`AddMonster`/`SetMonster`≈`MonsterCreate`/`MonsterSummonCreate`, `AddEffect`/`DelEffect`≈`EffectAdd`/`EffectDel`, `Teleport`≈`MoveUserEx`, `MoveGate`≈`MoveUser`, `Fireworks`≈`FireworksSend`, `PkLevelSend`≈`PKLevelSend`, `MoneySend`≈itself, `GetMapAttr`≈`MapCheckAttr`, `ChatTargetSend`≈itself) or depends on RoxGaming-specific systems/functions with no equivalent anywhere in this project under any name searched for (a whole `DeathSystem`/generic counter system, `gObjFindIndex`, `MoveObject`, `gObjDel`, `SetPositionMonsterRand`, `GCFireworksSendCustom`, raw-socket `gObjAdd`/`gObjAddSearch`, the whole Chaos Box Genesis item-creation family). Registered in `InitLuaFunction()`. Self-build-verified (`Release_EX603`/`Release_EX603CS`, 0 errors) and redeployed to `MuServer`.
- **Feature**: `LuaStack.hpp` - vendored the `luaaa` MIT-licensed C++/Lua binding helper (`LuaModule`/`LuaClass`, auto-marshals argument/return types via templates), ported from the "RoxGaming Main 5.2 - 60 FPS UPDATE" source pack. That pack links LuaJIT; this client vendors real Lua 5.2, so two changes from upstream: dropped its `<luajit\...>` include block (this client already gets `lua_State`/`luaL_*` project-wide via `stdafx.h`'s `lua/lua.hpp`), and mapped `luaL_mysetfuncs` to the real `luaL_setfuncs` for `LUA_VERSION_NUM > 501` (upstream only ever defined it for <=501, the branch LuaJIT always reports). First real consumer: `LuaBMD.cpp/.h`, a `BMD` class exposed to client Lua scripts (light color, `RenderBody`/`RenderMesh`, bone-relative `TransformPosition`, `CreateSprite`/`CreateParticle`/`CreateEffect`) - this project's `ZzzBMD`/`ZzzEffect`/`MapManager`/`CDirection` matched that pack's almost verbatim, so it's a near-direct port (dropped its `CCriticalSection` lock, a utility class this project doesn't have - this only ever runs on the main-thread render/update loop anyway). Registered in `ClientScriptLoader::Load()` alongside `InitClientLuaFunction()`. Build-verified by Evan.
- **Feature**: `LuaCharacter.cpp/.h` - exposes other nearby players (`CharactersClient[]`) to client Lua: name/level/class/guild, equipped-item model ids, position/angle (get+set), and outgoing trade/party/guild/shop requests. Also ported from the RoxGaming pack; two real bugs fixed along the way (not present in this project before, upstream-only): `SendRequestTrade` was called with an extra argument this project's 1-arg macro doesn't take, and `SendRequestOpenPersonalShop`'s shop-title argument was upstream reading the same Lua stack slot as the player index instead of the actual second argument. Registered in `ClientScriptLoader::Load()`. Build-verified by Evan.
- **Feature**: rest of the RoxGaming Lua binding port - `LuaUser.cpp/.h` (same as `LuaCharacter` but for the local player, `Hero`), `LuaObject.cpp/.h` (a generic `OBJECT`'s alpha/blend-mesh/animation state, plus a `WorldTime`-driven pulsing-value helper), `LuaItemObject.cpp/.h` (read-only `ITEM` wrapper: level/option1/exc), `LuaCloth.cpp/.h` (a character's cape/cloth physics slots, `CPhysicsCloth`), `LuaEffectsNormal.cpp/.h` (bone-relative `CreateSprite`/`CreateParticle`, fixed an upstream dead null-check that tested the address of a local parameter instead of its value). All registered in `ClientScriptLoader::Load()`. Build-verified by Evan.
  - Deliberately **not** ported from this pass, each for a specific reason:
    - `LuaEffects.cpp/.h` (the item-model-range-aware sibling of `LuaEffectsNormal`) - depends on `gItemManager.GET_ITEM_MODEL(x, y)`, a method this project's `CItemManager` (`ItemManager.h`) doesn't have (it's an empty stub). Porting it would mean guessing item-model ID ranges instead of verifying them.
    - `LuaOpenFolder.cpp/.h` and `LuaLoadImage.cpp/.h` - both are folder/file Lua-script loaders with their own embedded Lua state, redundant with this project's own `CClientScriptLoader`/`require()` system (`ClientScriptLoader.cpp`) and `LoadImage`/`RenderImage` bindings (`ClientLuaFunction.cpp`); `LuaOpenFolder` also needs the same missing `CCriticalSection` utility class `LuaBMD.cpp` already worked around.
- **Feature**: `LuaInterface.cpp/.h` - a general UI toolkit for client Lua: text rendering (fonts/colors via `g_pRenderText`), image/bitmap drawing (including a `RenderNativeImage` for the client's own built-in `BITMAP_*` UI assets, kept separate from this project's disk-loading `RenderImage`), native window show/hide (`g_pNewUISystem`), keyboard/mouse state, a full 3D item-icon preview (`RenderItem3D` inside its own camera/projection setup), sound (`DSPlaySound.h`), and chat/interface locking. Every symbol verified against this project's real headers first. Two real bugs fixed (upstream-only): `GetTextPosY` passed `sizeof(text)` - the pointer's own size - to `_GetTextExtentPoint32` instead of `strlen(text)`; `SetFontType`'s fallback case read a `gCreateFont.m_newFont` map this project doesn't have (dropped, falls back to the default font like every other build). Also collapsed upstream's three duplicate "RenderText" registrations and two duplicate box-width variants into two clear ones, `RenderText`/`RenderTextSimple`. Left out: the full item-tooltip renderer (`gItemManager.GET_ITEM` dependency, same blocker as `LuaEffects.cpp`), `LockPlayerWalk`/`UnlockPlayerWalk` and `GetWideX` (globals/functions this project doesn't have), the `GWidescreen`-based window-size getters (no such widescreen layer here), and `DisableClickClient` (needs a `gInterface.interfaceLock` this project doesn't have). Registered in `ClientScriptLoader::Load()`. Build-verified by Evan.
  - **Fix (build error from Evan)**: also dropped `VisibleHPPet` - `CNewUIItemEnduranceInfo` (`NewUIItemEnduranceInfo.h`) has no `m_isVisibleHPBar` field or equivalent in this project (that class always renders the pet HP bar unconditionally, no visibility toggle exists to expose). Earlier verification only confirmed the `g_pItemEnduranceInfo` accessor macro existed, not that the specific field on the class it points to did - real gap in the porting process, not just a naming difference.
- **Feature**: `LuaGlobal.cpp/.h` - game-data/settings queries for client Lua: window size, font size, resolution (get+set), item lookup by index (name/width/height/inventory slot, via the real `ItemAttribute` pointer-array this project has - `ZzzInfomation.h`), what item is under the mouse in the inventory, current map name, party size, monster name by class id. Deliberately **not** ported: `setLang()`/`SetLanguage()` (calls two hardcoded RoxGaming-binary addresses via inline asm - meaningless and unsafe here regardless of anything else - and reads/writes `g_aszMLSelection`, a global that doesn't exist in this project at all), `GetLanguage()` (same missing global, and `LuaInterface.cpp` already registers that Lua name via the real `g_strSelectedML`), `GetCompleteNameByIndex()` (hundreds of lines of hardcoded item-index constants tied to RoxGaming's own item database numbering - content data, not something verifiable against this project's source), `SetHealthBarSwitch`/`SetGlowSwitch`/`SetWingSwitch`/`SetFontValue`/`GetVolume` (dead no-op stubs upstream too - nothing real to port), and the duplicate `SendMessageClient(std::string)` (`LuaInterface.cpp` already registers that Lua name). Registered in `ClientScriptLoader::Load()`. Build-verified by Evan.
- **Revert**: Lorencia (`World1`) terrain and tiles restored to the original MU assets — reverts the customized tiles/terrain that had been in place, plus a full set of the stock `EncTerrain1`/`Terrain`/tile (`.att`/`.map`/`.OZJ`/`.OZT`) files.
- **Feature**: `LuaFunction.cpp/.h` (GameServer) - ~53 new `GetObjectX`/`SetObjectX` Lua bindings for `OBJECTSTRUCT` (`User.h`) fields that already existed natively but had no Lua binding yet. Found by surveying the "RoxGaming Main 5.2 - 60 FPS UPDATE" source pack's server-side `LuaUser.cpp` (2170 lines, an OOP-style `user:getX()`/`setX()` wrapper) field-by-field against this project's real `gObj[]`/`OBJECTSTRUCT` - of its 73 exposed fields, 66 already exist here verbatim (this project's struct is actually more complete: full elemental system, duel, siege/castle, gens, pentagram, muun inventory, none of which RoxGaming's `LuaUser.cpp` even touches). Only 7 fields don't exist here: 3 `ChaosBoxGenesis*` fields (a RoxGaming-specific event system this project doesn't have) and 4 `m_Language`/`m_OfflineMode`/`m_Patent`/`m_PatentType` fields (would need new struct fields + likely DB schema changes, not just a Lua binding - not attempted here). Build-verified (`Release_EX603|Win32`, 0 errors).
  - Added: missing setters for fields this project could already *get* but not *set* (`BP`/`MaxBP`, `Life`/`MaxLife`, `Mana`/`MaxMana`, `Shield`/`MaxShield`, `Authority`); full get+set for combat stats with no Lua binding at all (`Defense`, `DefensePvP`, `MagicDamageMax`/`Min`, `PhysiDamageMax`/`MinLeft`/`Right`, `PhysiSpeed`, `MagicSpeed`); warehouse/personal-shop/summon (`WarehouseCount`, `ReqWarehouseOpen`, `ExtInventory`, `ExtWarehouse`, `PShopOpen`, `PShopText`, `SummonIndex`); and the respawn-point system (`DieRegen`, `RegenMapNumber`/`X`/`Y`, `RegenTime`).
  - Pure data accessors only - no gameplay call sites were touched. The combat-stat pairs and the `Life`/`Mana`/`BP`/`Shield` setters intentionally have no `Type != OBJECT_USER` guard, matching this project's existing (already-shipped) unguarded getters for those same fields - usable on any `gObj[]` entry, not just players.
  - Deliberately **not** attempted this pass: `LuaGameServer.cpp` (RoxGaming's actual gameplay hook layer - `lua_getglobal("PlayerAttack"/"PlayerDie"/"PlayerMove"/...)` called from native C++ at combat/movement/trade/shop time). This project's `ScriptLoader.cpp` already has 8 of the same hooks under different names (`OnCharacterEntry`≈`EnterCharacter`, `OnMonsterDie`≈`MonsterDie`, `OnUserDie`≈`PlayerDie`, `OnUserRespawn`≈`RespawnUser`, `OnTimerThread`≈`TimerSystem`, `OnNpcTalk`≈`NpcTalk`, `OnCharacterClose`≈`DeleteCharacter`, `OnUserItemDrop`/`Move`≈`PlayerDropItem`/`PlayerMoveItem`); the genuinely new ones (`ChatProc`, the trade/shop/vault flow, `DeclareWar`/`GuildWarProc`) would each require finding and editing the matching native call site in this project's own GameServer, not just adding files - deferred to a future, narrower pass.
- **Feature**: `LuaFunction.cpp/.h` (GameServer) - 6 new item-introspection bindings rounding out `LuaInventoryGetItemTable` (which already read every `CItem` option field into one Lua table - turned out this project's read-side item API was already essentially at parity with the RoxGaming pack's whole `LuaInventory`/`LuaChaosBox`/`LuaTrade`/`LuaWarehouse` wrapper, which is the same `getX()`/`setX()` class copy-pasted across those four contexts). What was actually missing was the *write* side and a few state checks: `InventorySetItemOption` (writes `Option1`/`Option2`/`Option3`/`NewOption`/`SetOption`/`JewelOfHarmonyOption`/`ItemOptionEx`/`SocketOptionBonus`/`SocketOption[0-4]` by a `type` code, matching the RoxGaming pack's own numbering), `InventorySetItemDurability`, `InventoryConvertItem` (recomputes an item's cached defense/damage from its current options - call after `InventorySetItemOption` so stats actually reflect the change), `InventoryIsPeriodicItem`/`InventoryGetPeriodicItemTime` (rental items), `InventoryIsExcItem`. All verified against this project's real `CItem` (`Item.h`) first - every field/method matched exactly except the RoxGaming pack's `IsExtItem()` (this project's `CItem::IsExcItem()`, same concept) and its `IsExtLifeAdd()`/`IsExtManaAdd()` (pet/wing life-mana-bonus check), which don't exist anywhere in this project's `CItem` and weren't ported since there's no equivalent to verify against. Build-verified (`Release_EX603|Win32`, 0 errors).
- **Feature**: `LuaFunction.cpp/.h` (GameServer) - 9 more bindings for natively-existing functionality found while surveying the rest of the RoxGaming pack's `LuaFunctions.cpp`/`LuaGameServer.cpp`: `PartyCreate`/`PartyDestroy`/`PartyAddMember`/`PartyIsLeader`/`PartyIsMember`/`PartyIsParty` (this project's `CParty`, `Party.h`, already had `Create`/`Destroy`/`AddMember` natively alongside the `IsLeader`/`IsMember`/`IsParty`/`GetMemberCount`/`GetMemberIndex` already exposed - just no binding for the first three), and `QuestAddList`/`QuestInsertReward`/`QuestInfoSend` (`CQuest`/`CQuestReward`, `Quest.h`/`QuestReward.h`, same story - this project's existing `QuestStateCheck` binding only covers a `QUEST_FINISH`-specific check, these three round it out). Build-verified (`Release_EX603|Win32`, 0 errors).
- **Feature**: `ScriptLoader.cpp/.h` (GameServer) + `System/ScriptCore.lua` - new `OnChatProc(aIndex, text)` hook, fired on every chat message (including `/commands`) before this server's own command dispatch/broadcast; a script returning nonzero silently drops the message. This is the first hook ported from `LuaGameServer.cpp` (RoxGaming's gameplay-hook layer, `lua_getglobal("PlayerAttack"/"PlayerDie"/...)` calls into Lua from native C++) - traced its `ChatProc` all the way to its actual native call site (`Protocol.cpp`'s `CGChatRecv`), then confirmed this project has an identically-named `CGChatRecv` with a close-enough shape to hook the same way, before writing anything. Follows this project's own hook pattern exactly (`BridgeFunction_OnChatProc` in `ScriptCore.lua`, matching `OnNpcTalk`'s shape since both return a blocking value) rather than RoxGaming's. The remaining `LuaGameServer.cpp` hooks (`PlayerAttack`, `PlayerMove`, the trade/shop/vault flow, guild war) weren't traced to their native call sites this pass - each needs the same one-by-one investigation `ChatProc` got. Build-verified (`Release_EX603|Win32`, 0 errors) - chat and `/commands` need testing in-game.
- **Feature**: finished tracing the rest of `LuaGameServer.cpp`'s hooks. Turned out most of its registered hooks (`DeclareWar`, `GuildWarProc`, `PlayerVaultOpen`/`Close`, `CheckPersonalShopValue`, `PersonalShopAddValue`/`DecreaseValue`, `PlayerOpenShop`/`CloseShop`, `ItemBuyCheckCoins`/`BuyError`/`DecreaseCoins`, `ItemSellAddCoins`, `PlayerSellItem`) are **dead code in the RoxGaming pack itself** - implemented in `LuaGameServer.cpp` but never actually called from anywhere in its own native code, so there was no real call site to trace or mirror. Of the ones that *are* actually called: `PlayerSendTradeX`/`PlayerTradeXOk`/`TradeXReceiveCoins` all live in a `TradeX.cpp` this project doesn't have (a separate/alternate trade system, not present here at all); `PlayerBuyVaultOk` comes from `CWarehouse::CGWarehouseBuyRecv`, a "buy a vault slot" packet handler this project also doesn't have (this project's multi-warehouse feature works differently, entirely client-Lua-driven via `/bau`+`/abrirbau` - see `Lua-Source/Warehouse.lua`/`BauSelector.lua`); `PlayerAttack` and `PlayerMove` were traced and confirmed portable but deferred (PvP combat and per-move-packet frequency, higher risk, held back this pass).
  - Added the two that were both real and low-risk: `OnPlayerSendTrade(aIndex, bIndex)` (`Trade.cpp`'s `CGTradeRequestRecv`, fires right before the trade window actually opens) and `OnPlayerTradeOk(aIndex, bIndex)` (`Trade.cpp`'s `CGTradeOkButtonRecv`, fires right before a mutually-confirmed trade exchanges items/money - inserted right after the existing trade-duel special case, before the Pentagram/money/item validation cascade). Both follow the exact same hook pattern as `OnChatProc` (`ScriptLoader.cpp/.h` + matching `BridgeFunction_OnX` in `ScriptCore.lua`), return nonzero to cancel. Build-verified (`Release_EX603|Win32`, 0 errors) - trade requests and completed trades need testing in-game.
- **Feature**: `BauSelector.lua` — a "SELECIONAR BAU" panel (Panel9 stone/gold frame, title + "Bau atual: N" subtitle, 11-button 0-10 grid with the current vault highlighted) replacing the plain-text `Warehouse.lua` picker as the client-side UI for the existing `/bau <num>` + `/abrirbau` commands; reuses that script's already-verified command-sequencing logic (chat-cooldown polling, "no interface open" guard) unchanged.
- **Feature**: two new client Lua bindings, `DrawTextCentered`/`DrawTextBigCentered` (`ClientLuaFunction.cpp`) — exact text centering via the engine's own `RT3_SORT_CENTER` layout (`RenderText()`'s box-width/sort params) instead of a guessed character width, and a 2x bold font (`g_hFontBig`, already created at startup but never exposed to Lua) for standout labels like a digit grid.
- **Feature**: reusable `Panel9.lua` 9-slice panel helper (`Lua-Source/System/Panel9.lua`) for custom Lua UI windows — `Panel9.Draw`/`Panel9.DrawCentered(x/w, y/h, cornerSize, skin, colors)`, with a single-fill fast path for a single photographic panel texture (stretched whole, no seam) and a flat-color fallback for skin fields with no art yet. `Panel9Demo.lua` is the reference consumer (toggle button, real Midjourney-sourced stone/gold skin, functional close button, drag-and-drop-placed extra elements).
- **Feature**: `Tools/ImageConverter/png_to_mu.py` / `remove_bg.py` / `mu_decode.py` — convert any Pillow-readable image to/from this client's `.OZT`/`.OZJ` formats (spec reverse-engineered from `GlobalBitmap.cpp`, round-trip verified), plus a connectivity-aware (flood-fill) background remover for "isolated on black" Midjourney source art that preserves legitimately-dark enclosed design areas.
- **Feature**: `Tools/PanelDesigner/panel_designer.py` (+ packaged standalone `Panel9LayoutDesigner.exe`) — a visual editor for laying out a `Panel9`-based window (drag assets onto background/close-button/canvas to create buttons, resize, undo history, live-generated Lua) and saving straight into `Panel9Demo.lua`, instead of hand-editing coordinates.

### 2026-08-16
- **Fix**: client Lua's `LoadImage()`/`RenderImage()` rendered a plain white square for any non-power-of-2 `.OZJ`/`.OZT` UI image (e.g. `whare_off.ozj`, `newui_Bt_Warehouse.OZT`) no matter what was tried - real root cause was `Bitmaps.LoadImage()` returning `BITMAP_UNKNOWN` (`_TextureIndex.h`: `= 30000`, *not* `0`) on a failed load attempt; the code was checking `slot == 0`, so a failed `.jpg` try was never detected and the client's placeholder "unknown texture" got drawn and stretched instead. Two earlier real issues surfaced and got fixed along the way to get here: `RenderBitmap()` doesn't enable `GL_BLEND` on its own (`EnableAlphaTest()`/`DisableAlphaBlend()` now wrap the draw), and non-power-of-2 source art gets padded to the next power of 2 at load time with uninitialized memory, not transparency (native code crops this with a literal UV fraction, e.g. `24.f/32.f` - see `UIControls.cpp`/`ZzzInventory.cpp`'s `BITMAP_INVENTORY_BUTTON`).
- **Feature**: `LoadImage(path)` no longer needs a `.jpg`/`.tga` extension, or even to know which one an asset actually is - it tries both real on-disk formats (`.OZJ`/`.OZT`) itself and uses whichever exists. It also measures the image's real (pre-padding) size and returns it (`uWidth`, `vHeight`); `RenderImage(id, x, y, w, h)` looks this up automatically and crops accordingly, so scripts never need the `24.f/32.f`-style manual math native code uses.
- **Chore**: `Lua-Source/encrypt_lua.py` now deploys the encrypted `.enc` straight to `Global Debug\Lua\` instead of dropping it next to the source and requiring a manual copy step (`--local` still available for a loose copy alongside the source, rarely needed).

### 2026-08-15
- **Feature**: Lua scripting engine embedded in the GameServer (`ScriptLoader`/`LuaFunction`, ~170 native bindings covering objects, items, monsters, guilds, commands, effects, messages), ported from a compatible SSeMU-lineage pack and adapted to this project's real API surface. Vendors Lua 5.2 (headers + a prebuilt `lua52.lib`). Scripts live under `MuServer/Data/Script/` (`ScriptMain.lua` → `System/ScriptCore.lua` core dispatcher + individual scripts); a new `Script\WindowTitle.lua` sets the client's window title to `Player: <nick> || Level: <level> || Reset: <reset>` on character entry, using a new `SetObjectWindowTitle` Lua binding and a matching new client↔server packet (`0xC1:0x74`).
- **Fix**: the window title (above) only refreshed at login, so leveling up mid-session left it showing a stale level. Added a proper `OnUserLevelUp` script hook (fires the instant `CharacterLevelUp` succeeds) instead of relying on login-only data. Confirmed fixed in-game.
- **Feature**: GameServer console menu — `Recarregar → Recarregar: Script`, reloads just the Lua scripts (`ScriptMain.lua` and everything it requires) without touching any of the other `Recarregar: *` categories. `Recarregar Todos!` already covered this since scripts reload as part of `ReloadAll()`.
- **Feature**: a second, independent Lua scripting engine embedded in the client (`Source_Client`) - `ClientScriptLoader`/`ClientLuaFunction`, hooked into the real per-frame UI update/render loop (`g_pNewUISystem`'s `Update()`/`Render()` in `ZzzScene.cpp`). Scripts live under `Lua-Source\` (tracked source) → built into `Global Debug\Lua\` (deployed). First script (`TestButton.lua`) draws a clickable text button and posts a chat message on click - confirmed working end-to-end in-game.
- **Feature**: client Lua scripts ship encrypted (`.enc`, XOR + a magic header - the same protection tier as this client's `.bmd` data files) instead of as plain `.lua`, so the deployed client folder doesn't leak readable script source. `ClientScriptLoader` loads `.enc` if present, falling back to a plain `.lua` for local dev iteration. `Lua-Source/encrypt_lua.py` is the build step; see `Lua-Source/README.md`.
- **Feature**: multi-warehouse UI (`Lua-Source/Warehouse.lua`) - built entirely on top of `/bau <num>` and `/abrirbau`, two GameServer commands (`CommandManager.cpp`) that turned out to already be fully implemented and configured (per-account-level vault limits, an `ExtWarehouse` DB table, the whole DataServer round trip) but never had a client UI and were easy to miss since command registration in this file is data-driven, not the commented-out `this->Add(...)` calls next to each one. A small `[Trocar Bau]` trigger (only shown while the warehouse is closed, since `/bau` refuses to run while any interface is open) opens a 0-10 picker; picking a number sends `/bau N` then `/abrirbau` once the chat cooldown actually clears - opens the chosen vault from anywhere, no NPC needed. Added a handful of new client Lua bindings along the way: `DrawPanel` (flat background, reusing `NewUIMuHelper`'s own `RenderColor` primitive), `ConsumeClick` (stops a UI click from also moving/attacking - has to clear both `MouseLButton` and `MouseLButtonPush`, `ZzzInterface.cpp`'s `Attack()` checks either), `GetChatTime` (polls the real `SendChat()` cooldown instead of guessing a fixed wait).
- **Feature**: raw SQL access from server-side Lua scripts (`SQLConnect`/`SQLQuery`/`SQLFetch`/`SQLGetNumber`/`SQLGetString`/etc, 10 bindings) - restored by porting `QueryManager.cpp/h`, a standalone ODBC connection class, straight from this project's own DataServer (it already has this exact class for its own use). Connects once at startup from `GameServerSQL.ini` (`[SQL] ODBC=/USER=/PASS=`, not tracked in git); if that file is missing or empty, SQL is just unavailable to scripts - everything else still starts up fine.
- **Feature**: `MuServer/Data/Script/System/ScriptDefine.lua` - named constants (`ObjectType.OBJECT_USER` instead of a bare `1`) for the ~180 native Lua functions, covering objects, items, effects, maps, messages, and now `SQLResult` too. Sourced from a community-maintained list for this SSeMU-lineage Lua API, trimmed to what applies here.
- **Feature**: MU Helper auto-play engine ported in full (attack, heal, buff, drain life, item pickup/filtering, equipment repair, party support, combo skills, self-defense), with a gear/play icon pair on the position HUD and a `Z` hotkey to open it.
- **Feature**: MU Helper config (skills, range, repair, pickup filters, thresholds) now persists across logins, server-side — reused this pack's existing but previously client-unused `HelperData` DB pipe (`0xC1:0xAE`/`0xC1:0x17`) instead of a bespoke mechanism.
- **Fix**: MU Helper's repair-item check flooded a request every 250ms tick during combat — throttled to once every 5s.
- **Fix**: MU Helper's "Pick All Near Items" / "Pick Selected Items" were only visually mutually exclusive — the underlying flags never actually cleared, so it kept picking up everything regardless of the UI.
- **Fix**: closing MU Helper left its skill-picker sub-window open (`CNewUIObj::Show()` isn't virtual, so the generic window-close path couldn't reach the override that closes it).
- **Fix**: MU Helper's "Add Extra Item" truncated names to 14 characters (e.g. "Scroll of Hellfire" → "Scroll of Hell") — two separate leftover buffers, both from a dropped older protocol format.
- **Fix**: MU Helper's saved-item list couldn't select any line but the first, and allowed duplicate entries.
- **Fix**: MU Helper's Auto Potion/Heal/Party-Heal threshold bars reset unless closed via the exact right button.
- **Fix**: Dark Spirit pet never actually attacked in any mode — was sending the raw command enum value instead of the small index the server expects.
- **Fix**: several skills (Twisting Slash, Evil Spirit/Storm, and ~30 others across every class) dealt damage via MU Helper but never played their animation — ported each one's real client-authoritative packet sequence from its own source location.
- **Fix**: MU Helper's basic attack ignored the character's actual attack speed, attacking every 250ms tick regardless of Agility — now paced using the same animation-duration data the game itself uses to play the swing.
- **Fix**: skill names (character skill list tooltip) were in Portuguese in *both* the Portuguese and English clients — translated all 365 to English in both.
- **Fix/Translation**: Command Window (`D` key), several item-stat tooltip lines (e.g. "Reflect Damage"), a few special-item descriptions, all 158 buff names/descriptions, and the Guild creation prompt were in English in the Portuguese client — translated to Portuguese.
- **Feature**: screenshots (`PrintScreen`) now save into a `Screenshots\` folder (created automatically if missing) instead of the client's root, and the filename now includes the character's nickname.
- **Chore**: stopped tracking `Source_Client/Global Debug/Main` — it was a stale 19MB `.ilk` (incremental linker database) file mistakenly committed instead of the real `Main.exe` (it dodged the `.gitignore`'s `*.exe`/`*.ilk` rules by having no extension). The real `Main.exe` stays untracked, as it already was.

### 2026-08-14
- **Feature**: skill use locked out for 1.5s right after the SM's Teleport skill, to stop it being used to chain into other skills faster than normal.
- **Fix**: mailbox letter-read crash (`MemBlock==0xFD` assert) — `ReceiveLetterText` wrote a null terminator past the received packet data when the letter's memo length exactly matched the packet size.
- **Feature**: F9 — CTRL Lock, keeps CTRL reporting as held (see names/attack without holding the key), persists across sessions.
- **Feature**: F12 — minimize to system tray with a tray icon; restoring is double-click-the-icon only (safe with multiple client instances running at once).
- **Fix**: `/addpoint` (`/v /a /s /e /c`) requiring a relog to show added stat points — now confirms immediately via the same packet the native "+" button uses, and no longer floods/disconnects on large amounts (e.g. `/a 1000`).
- **Fix**: Life/Mana/Shield/BP/LevelUpPoint no longer wrap or cap at 65000/65535 (widened `WORD`→`DWORD` across the relevant packets, server and client).
- **Fix**: widescreen "black triangle" corners on the terrain at wider-than-4:3 resolutions (e.g. 1600x900) — the terrain visibility test was always sized for a 4:3 view regardless of the real screen aspect ratio.
- **Feature**: Camera3D scroll-wheel zoom clamped to `[25, 40]` FOV in Release builds (ported from the sibling `project_mu_main52_63`).
- **Fix**: "Now Loading..." screen only filling the original 800x600 area instead of the whole window on higher resolutions.
- **Fix**: `/post` command producing garbled text (e.g. `"ÁN"`) — a `wsprintf` call was missing one of its two format arguments, reading uninitialized memory.
- **Chore**: stopped tracking `PacketList.txt` and `Source_Client/Global Release/` (runtime debug log and a 1GB+ build output, neither is source).
